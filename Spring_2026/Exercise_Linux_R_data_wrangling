
# 🧪 Excercise on Linux, R, and Data Wrangling

This assignment will test your current knowledge of **Linux commands**, **R functions**, and your **data wrangling skills**.

---

## 📌 Questions

### **1. Working Directory**
What is the command for showing your current working directory:
- in **Linux**?
- in **R**?

---

### **2. Pipes**
- What does a *pipe* do?
- What is the pipe symbol:
  - in **Linux**?
  - in **R**?

---

### **3. Using `cut` in Linux**

#### **3a. Command comparison**

Considering the file `tags1.txt` from "Linux Practical 1 - Linux commands", what is the difference between the two commands below?

```bash
cut -f1 tags1.txt
cut -f1,2 tags1.txt
```

#### **3b. Conceptual understanding**

In general, when do you think the command `cut` is useful?

---

### **4. Identifying R functions**

Which of the following is an **R function**?

- a) `c[A:Z]`  
- b) `list.files()`  
- c) `path[]`  
- d) `dat.`  

How can you tell what each of these represents?

---

### **5. Data structures in R**

What is the difference between a `data.frame` and a `tibble`?

---

### **6. Function `near()` in tidyverse**

In the script:

https://github.com/bioinfo-arctic/FSK2053/blob/main/Spring_2026/Practice_2.2_Data_wrangling_one_dataset.md  

- What is the function `near()` used for?
- What is the advantage of using it?

---

### **7. Joins in tidyverse**

In the **tidyverse** package, what is the difference between the following functions?

- `inner_join()`
- `left_join()`
- `right_join()`
- `full_join()`

Reference: [Joins in R for Data Science](https://r4ds.hadley.nz/joins.html#sec-mutating-joins)

---
